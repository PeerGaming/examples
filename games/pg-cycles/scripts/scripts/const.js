
// elments => minmal size, see visual figures

//full screen, - paddng, offset

var ELEMENT_SIZE  = 20;

    OFFSET = ELEMENT_SIZE * 2,

    FIELD_WIDTH   = window.outerWidth   - OFFSET, // 600
    FIELD_HEIGHT  = window.innerHeight  - OFFSET; // 400,


